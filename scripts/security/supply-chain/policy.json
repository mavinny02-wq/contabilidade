{
  "schema_version": 1,
  "fail_severities": ["HIGH", "CRITICAL"],
  "exception_required_fields": ["id", "rule", "resource", "owner", "reason", "expires"],
  "redact_fields": ["secret", "token", "password", "authorization", "payload"],
  "tools": {
    "semgrep": "1.136.0",
    "trivy": "0.66.0"
  }
}
